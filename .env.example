# AIMD environment configuration
# Server-side only. Never expose these to the browser (no VITE_ prefix).

# Hive AI — AI-generation, deepfake and AI-audio detection.
# Without this key the detection provider truthfully reports "unavailable".
HIVE_API_KEY=

# Optional SynthID verification endpoint (no public API today).
SYNTHID_API_URL=
SYNTHID_API_KEY=

# Provided automatically by Lovable Cloud:
# LOVABLE_API_KEY      - AI reasoning over normalized evidence
# SUPABASE_URL         - backend URL
# SUPABASE_PUBLISHABLE_KEY
# SUPABASE_SERVICE_ROLE_KEY
